# 版本记录

## 1.4.6

**二~六级的「编号序列」多一个下拉框：`继承父标题编号`（默认）/ `重新编号`**

- `继承父标题编号` 就是原来的行为，编号接在父级后面。
- `重新编号` 让**本级从 1 开始、不带父级前缀**；比它深的级别仍然拿它当父级，照常往下拼。
  例：四级选「重新编号」→ `#### 1 四级标题`、`##### 1.1 五级标题`、`###### 1.1.1 六级标题`。
- **一级没有这个下拉框**：一级没有父级，「继承 / 重新编号」是同一件事。
- 跳级时行为不变：文里没有四级 / 五级标题时，六级往上找最近的父级（三级）→ `1.1.1.1`。
- 老设置没有这个字段 → 一律按「继承父标题编号」，行为与之前**逐字一致**。
- 测试从 338 项加到 **353 项**。

## 1.4.5

**新增「恢复默认设置」（设置面板最下面一行、靠右）**

- 带 ↺ 重置图标的按钮，点了**弹窗确认**：`是否要恢复默认设置？`，**确定在左、取消在右**
  （用户指定，与 Obsidian 自带「主按钮在右」的惯例相反）。
- 确认后把所有设置恢复成默认值 —— 等同于刚装好、还没动过任何选项的状态：自动编号关闭、检测间隔 10 秒、
  各级规则回默认、旧编号处理与子标题继承回默认。
- **语言不在重置范围内**：它是界面偏好，重置后保留你当前的选择；否则面板会在你点完的瞬间整个变成另一种语言。
- 重置时一并复位兜底时钟与内部记账（标题签名作废），所以重置后下一次编号就是按默认规则走。
- 测试从 315 项加到 **338 项**。

## 1.4.4

**首装自动跟随 Obsidian 的界面语言**

- 之前默认语言写死 `zh-CN`，英文用户首装看到中文界面。现在**首装时会识别 Obsidian 的界面语言**：
  中文 → 简体中文，繁中（含港 / 澳）→ 繁體中文，其它语言 → English。
- **一旦你自己在设置里选过，就以你的选择为准，不再被覆盖。**
- 识别结果不落盘：没动过设置就一直跟着 Obsidian 走；改过任何设置之后语言才固定下来。
- Obsidian 没把界面语言放进插件 API，实现上是读 `<html lang>`（实测本机是 `'zh'`）为主、
  `localStorage.language` 优先、`moment.locale()` 兜底。**不用 `navigator.language`** —— 那是系统语言，
  与 Obsidian 的设置无关（细节见 DEVELOPMENT 承重项 §15）。
- 测试从 299 项加到 **315 项**。

## 1.4.3

**改标题前的预检闸门：链接同步不可用就一行都不改（用户 2026-09-24 拍板）**

- 新增预检：**写入之前**先检查链接同步要用的两个 Obsidian 内部接口是否都在。不在就**整轮停手**、
  一行业都不改，并提示 `Heading Guard:无法同步链接，标题已停止更改`。
  理由：那两个接口改的是别的文件、异步、非公开，Obsidian 没有跨文件事务 —— 先改标题再同步，
  一旦同步失败就留下「标题改了、链接还指向旧名」的坏状态，事后回滚也只能尽力而为。
- **「重置当前笔记标题编号」走同一条链路，用同一个闸门。**
- 自动编号下这条提示**只弹一次**（否则定时器每轮都弹、刷屏）；接口恢复后再出问题会重新提示一次。
  手动命令每次都提示。
- ⚠️ **它挡不住「接口在、但调用后没真正生效」那种静默失效** —— 那类失败与「一切正常」的可观测量
  完全相同，原理上检测不了。这种情况仍可能留下「标题改了、链接没跟着走」，如实说明。
- 一个有意的副作用：接口缺失的 Obsidian 版本上，本插件对编号而言变成**只读** ——
  连完全没有链接的标题也不会改。这是「宁可不改，也不制造断链」的取舍。
- 测试从 290 项加到 **299 项**。

## 1.4.2

**「%%skip%%」那一行现在也会被删掉旧编号；选项改名为 `子标题是否继承父标题的跳过注释`**

- 选项名：`注释跳过的标题的子标题是否继续跳过` → **`子标题是否继承父标题的跳过注释`**（用户 2026-09-24 指定）。
- **行为修正**：把「尝试删除旧编号」的作用范围从「只有继承跳过的子标题」扩大到**这一档覆盖到的所有行** ——
  带 `%%skip%%` 的那一行**自己**也会被删掉旧编号（只删、不写新号）。
  之前它被需求文档 §3.5.1「即使它自身已有编号，也不去改变或删除」挡着，于是出现
  「子标题的编号没了、父标题还挂着 `3.3.2`」的不一致 —— 用户实测报的问题。
- ⚠️ **§3.5.1 的保证没有被推翻，只是加了条件**：选默认的 `不删除旧编号` 时，带 `%%skip%%` 的那一行**仍然完全不动**；
  只有显式选 `尝试删除旧编号` 时才动它。另外「子标题继承」开关关着时，就算存量值是 `尝试删除旧编号` 也不生效。
- 顺带修掉 `planReset` 里一行重复的判断（死代码，行为无变化）。
- 测试从 285 项加到 **289 项**。

## 1.4.1

**「全局编号尾缀」去掉开关（用户 2026-09-24 拍板）**

- 原来这一行是「开关 + 尾缀字符 + 尾缀后空格」三个控件。开关与下拉框里的 `无` 本来就是同一件事，现在只留下拉框。
- **选 `无` = 不用全局尾缀** → 回落到一至六级标题各自的尾缀设置，各级控件恢复可编辑。
  选了任何一个符号 = 全局生效 → 覆盖一至六级，各级的尾缀控件（**含开关**）全部变为不可编辑。
- 「尾缀后是否加空格」这个下拉框也接上了：全局选 `无` 时它不可编辑（此时它不起任何作用）。
- 老设置里的 `globalSuffix.enabled` 字段**既不再读、也不再写**。⚠️ 如果你以前是「开关打开 + 字符选 `无`」（效果 = 全局只补一个空格），升级后会变成「不用全局尾缀」，由各级自己的设置决定 —— 按你的口径这两者本来就是同一件事。
- 默认配置（`无` + 加空格）下的编号结果与 1.4.0 **完全一致**。
- 测试从 276 项加到 **285 项**。

## 1.4.0

**新选项：`注释跳过的标题的子标题是否继续跳过`（在「编号前尝试去除旧编号」下面）**

- 一个开关（**默认关闭**）+ 一个下拉框（`不删除旧编号`（默认）/ `尝试删除旧编号`）。
- 打开后：带 `%%skip%%` 的标题，**它下面的整棵子树一起跳过** —— 不编号、不占号，
  直到出现等级 **≤** 那个标记标题的标题为止（兄弟和更上层的标题都不受影响）。
- 下拉框选 `尝试删除旧编号` 时，这些**继承跳过**的行会顺手把旧编号删掉（只删、不写新号），
  用的是同一个 `stripOldNumber`。
- ⚠️ **带 `%%skip%%` 的那一行本身依然完全不动**（文档 §5.1「不做任何动作」），
  即使下拉框选了「尝试删除旧编号」也不删它自己的旧编号。
- 关闭时行为与之前**完全一致**（子标题照常编号）。
- 老设置里没有这两个字段 → 自动补成「关闭 / 不删除旧编号」，行为不变。

## 1.3.2

**两条用户可见提示**

- 编号完成：`N条标题已自动编号`（原来的「已更新 N 个标题编号」）
- 有链接跟着改：`更新M个文件中的N条链接`；**没有链接要改时不弹**这条（不报 0）
- 命令「重置当前笔记标题编号」同样弹第二条（第一条仍是「已重置 N 个标题编号」——它不是「自动编号」）。
- 计数口径：**只数 Markdown 里真正被改写的引用**。Canvas 嵌入卡片不计入（那部分由 Obsidian 自己的
  提示汇报，比如 “Canvas updated N embed cards.”），这与官方那条提示的口径一致。
- 自动触发（切笔记 / 停手 / 兜底时钟）仍然静默 —— 否则每 10 秒弹两条提示。

## 1.3.1

**链接比对对齐官方：大小写不敏感**

- 官方匹配引用时两边都 `.toLowerCase()`（`LT(子路径).toLowerCase() === LT(旧标题).toLowerCase()`），插件在此之前是区分大小写的
  —— 于是 `[[甲#ABC]]` 指向标题 `abc` 时官方会同步、插件不会。现已对齐。
- 真机验证：`[[甲#ABC]]` + 标题 `abc` → 编号后链接跟着改成 `[[甲#1、abc]]`。
- ⚠️ **只在这一处 lowercase，没动 `normHeading` 本身**：那个函数还被「陈旧保护」共用，
  一旦它也不区分大小写，编辑器里的 `## ABC` 与缓存里的 `abc` 会被判为同步，插件就会按缓存的拼写把标题文本改掉（静默改大小写）。
  离线测试同时锁住了这两条（比对不敏感 + `normHeading` 保持敏感）。

## 1.3.0

**「去旧编号」吃掉连续空格**

- 按空格切的时候，把**整段连续空格**一起吃掉：`## 1     图形化环境` → `## 图形化环境`（原来会剩下 `##     图形化环境`）。
- 只有空格这一条会这样做；符号切点不受影响。

**设置面板：一~六级标题可折叠，默认收起**

- 每级用原生 `<details>` / `<summary>` 包起来，**默认全部折叠**，点标题展开。面板从「一屏塞不下」变成「六行标题 + 展开你关心的那一级」。
- 三角是自己画的 CSS `::before`，不用 `::marker`（主题常把 marker 关掉，各主题表现不一致）。

**跳过编号时也能删旧编号：每级多一个下拉框**

- 每级「跳过编号」开关后面加了一个下拉框：**`不删除旧编号`**（默认）/ **`尝试删除旧编号`**。
- 打开「跳过编号」下拉框才可编辑；关掉则不可用。
- 选 `不删除旧编号` = 与原来的行为完全一致（整级不动）。
- 选 `尝试删除旧编号` = 这一级仍然**不写编号**，但会把标题里已有的旧编号删掉再重命名（用的还是同一个 `stripOldNumber`）：
  `## 1.2、 二级标题` → `## 二级标题`。
- 老设置里没有这个字段 → 自动补成 `不删除旧编号`，行为不变。

## 1.2.3

**按你 2026-09-24 重新给定的统一规格改造「去旧编号」——两个命令共用同一个算法**

- **切分符号表**改为：`、` `-` `)` `_` `>` `」` `』` `;` `~` `】`（**去掉了 `.`**）。
  于是 `3、78.21线` 从 `、` 切开得到 `78.21线`，而不是从 `78.` 切开得到 `21线`（你给的例子 5）。
  —— 顺带说明：`. ` 正是上次实测误删（`2.6 78.6位` → `6位`）的根因，你直接把源头修了。
- **中文范围**从 `[\u4e00-\u9fa5]` 扩到 `[\u4e00-\u9fff]`（覆盖扩展区）。
- **两个命令合并成同一个算法**：`stripOldNumber(text)` 不再分模式。
  「编号前尝试去除旧编号」与「重置标题编号」都调它，行为完全一致。
- 中文数字白名单仍是 20 个（一…九、壹…玖、十、拾）；「连续两个英文字母」与「别把标题清空」两道守卫保留。
- **单刀是递进的**：`## 3.1.1.1 (1) 测试四级标题` 按一次得到 `## (1) 测试四级标题`，再按一次才得到 `## 测试四级标题`。
  （你更早那条例子期望一次到位；一次到位只能靠迭代，而迭代已在 1.2.2 被实测证伪。）

## 1.2.2

**回退我自己加的那层「反复切」，两条「去旧编号」改成「一份实现 + 显式切点」**

- 背景：为了同时满足你的两条规格（重置命令=最后一个空格、去旧编号=第一个空格），我给重置命令套了
  「反复切到切不动为止」。**这是我自己的发明，你从没要求过。**
- **拿数据证伪**：全库 242 篇笔记、**1774 个真实标题**跑一遍，反复切会多切 12 条，其中至少 4 条是确凿误删 ——
  `2.6 78.6位` → `6位`（吃掉 `78.`）、`(1) 8 种基本数据类型…` → `种基本数据类型…`（吃掉 `8`）、
  `(2) 3 种引用数据类型…` → 同、`t10 - 获取实时行情` → `获取实时行情`。
  **根因**：`3.1.1.1 (1) ` 与 `8 ` 在文本上结构同形，迭代永远分不清「这里是编号」还是「这里是正文」。
- 现在：`stripOldNumber(text, mode)` —— **一份实现、共用全部守卫、只切一刀、不迭代**。
  - `mode='first'` → 自动路径（你的「优先第一个空格」规格）
  - `mode='last'` → 重置命令（你的「最后一个空格」规格）
- §9 把那 4 条实测误删钉成了回归断言。

**全库审计（1774 个标题 × 两种模式的每一次命中）**

| 模式 | 命中 | 被切部分含数字/中文数字 | 其中「纯数字+空格」（最像正常标题） | 其它 |
| --- | --- | --- | --- | --- |
| `first` | 746 | 736 | 9 | **0** |
| `last` | 311 | 306 | 5 | **0** |

「其它 = 0」= 没有任何一次删除切掉的是「既无数字、又无编号标点」的纯字母片段。
9 条「纯数字+空格」逐条看过：8 条确实是编号（`2 样例数据`、`3 java-client`…），
**只有 1 条可疑**：`2 ch, 11 dc (12 sts)`（编织图解，`2` 有可能是正文）。
这是启发式规则不可消除的残余风险，已写进 README 的已知限制。

**效率实测**：`first` 0.23 µs/次、`last` 0.11 µs/次；作对比，整轮编号规划（40 个标题）7.0 µs ——
剥离编号对性能没有可测影响。

## 1.2.1

**修复：加上 `%%skip%%` 再删掉之后，那个标题再也编不上号（用户实测报告）**

- 根因：`%%skip%%` 写在标题的**下一行**、不属于标题文字，而「标题签名」只算了等级 + 顺序 + 内容 —— 加/删标记时签名**一模一样**，签名门禁直接 `return`，于是永远等不到重新编号。
- 现在签名把「下一行有没有 `%%skip%%`」也算进去；凡是编号流程会读的东西都必须进签名。
- 顺带修掉同一类问题：改设置（换间隔符/尾缀等）之前也要等「切笔记」才生效，现在改完立刻按新规则处理当前笔记。

**修复：只改标题「下一行」的编辑触发不了重新编号**

- 层②原来只听 `metadataCache.changed`。但加/删 `%%skip%%` 只改下一行的文字，缓存的元数据一模一样，Obsidian **不会**触发 `changed` —— 只能干等兜底时钟（最长 60 秒）。
- 现在同时听 `editor-change`（与元数据事件共用一个 1 秒防抖计时器，不会跑两遍）。实测删掉标记后约 2.6 秒就重新编号。

**修复：因为缓存落后而整轮放弃的那一次编辑会被丢掉**

- 那一轮不写文档，但签名已经被记账 → 后续触发全都认为「检查过了」→ 这次编辑永远等不到处理。
- 现在放弃时会记一个「还欠一次」，下一次触发不看签名补跑一遍，直到真的跑成功。

**界面调整（按你的要求）**

- 一至六级标题的「编号尾缀」：本级开关关掉后，字符与空格两个下拉框都变成不可编辑。
- 删掉样式预览框的说明文字。
- 「编号前尝试去除旧编号」的说明改为：`编号之前，尝试删除旧编号，支持的格式有限，必要时请手动删除后重新编号`。
- 「开启自动编号」的说明改为：`当光标出现时，自动为当前笔记标题编号`；并把检测间隔滑块、秒数、重置按钮**合并到同一行**，删掉检测间隔自己的标题与说明。
- 「全局编号尾缀」的说明改为：`为标题编码末尾添加尾缀符号，例如`1.2、`，顿号就是尾缀`。
- 设置面板加了一条看得清、拖得住的原生滚动条（主题常把滚动条做没，导致「滚得动却找不到拖的地方」）。

**「重置当前笔记标题编号」与「编号前尝试去除旧编号」合二为一**

- 重置命令改为复用同一套算法，不再维护两套「去旧编号」。
- 但重置命令会**反复切到切不动为止** —— 编号本来就会分层（`3.1.1.1 (1) 测试四级标题` 里有两截），切一刀只去掉 `3.1.1.1 ` 会剩下 `(1) 测试四级标题`。
- 副产物：`## 1.1 Beta Gamma` 现在也不会再被吃成 `## Gamma` 了（新算法按第一个空格切，且拒绝连续两个英文字母）。

## 1.2.0

**设置项改成「候选锁死」的下拉框**

- **编号尾缀**（全局 + 一至六级）：不再是输入框，改成下拉框，只能在 `无`、`、`、`.`、`·`、`-`、`_`、`:`、`)`、`>`、`」`、`』`、`;`、`~`、`】` 里选。默认 `无`。
- **编号层级间隔符**（一至六级）：改成下拉框，只能在 `无`、`.`、`-`、`·`、`_`、`:`、`~`、`<`、`>` 里选。默认 `.`。
- **前后缀括符**（一至六级）：改成下拉框，只能在 `无`、`()`、`（）`、`<>`、`《》`、`「」`、`『』`、`【】` 里选。默认 `无`。
- 老设置里的旧值（`text` 字段、`(|)` 这类自由文本）会自动迁移或回落到 `无`，不会留下非法值。

**自动编号：检测间隔可调 + 三层触发**

- 设置里新增**检测间隔滑块**（5~60 秒，默认 10）与**重置按钮**（关闭自动编号并把间隔恢复为 10）。关闭自动编号时滑块不可用。
- 触发机制从「死板轮询」改成三层：
  1. 切到某篇笔记 / 光标落进编辑器 → **2 秒后立刻编号一次**；
  2. 当前笔记的标题元数据发生变化 → **防抖 1 秒后编号**（停手约 1 秒就编好，不用等一个周期）；
  3. 轮询兜底（间隔由滑块决定）：先比对**标题签名（等级 + 顺序 + 内容）**，一模一样就什么都不做。

**新增：样式预览框**

- 设置面板里新增只读的样式预览，把固定的六行样例按当前规则跑一遍，改任何设置都会立刻刷新。

**改造：`自动去除旧编号` → `编号前尝试去除旧编号`（逻辑全部重写）**

- 改名以消除歧义。
- 新规则：先按「第一个空格」切（紧贴行尾的空格不算）；没有可用空格就从后往前找 `、` `-` `.` `_` 切一刀。
  切下来的部分要像编号才删：不能含「一…九、壹…玖、十、拾」之外的中文，也不能含连续两个英文字母。
- 例：`1.2、 一级标题` → `一级标题`；`1.2、一级标题` → `一级标题`；`aB、 一级标题` 不动；`1.2、一级-标题` 不动。

## 1.1.1

**修复：`自动去除旧编号` 在某些标题上不生效（用户实测报告）**

- 触发条件：严格剥离只吃掉了**当前编号**，剩下的文字里还压着更早的旧编号。
  例：`## II 1、 设置界面` —— `II` 是当前编号，`1、 ` 是残留；严格剥离后剩 `1、 设置界面`，
  而用当前编号 `II` 拼回去恰好等于原文。
- 旧实现把去旧编号挂在「拼装结果与原文不同」这个条件上 → 条件不成立 → 清理永远不执行，
  标题卡在 `II 1、 设置界面` 这种双编号状态。
- 现在改为：只要开关打开，剥离当前编号之后**无条件**再做一次残留清理。
- 残留必须以「带标点的编号片段」形态出现（`1、`、`1.2`、`(1)`、`一、`）才会被切掉；
  顺带锁死两条安全边界：`## 1.1 Beta Gamma`、`## 1.1 2024 年度总结` 都不会被吃掉后面的文字。

**已知交互（不是 bug，文档 §5.1 就是这么定的）**

- 某一级打开「跳过编号」后，该级标题**整行都不做任何动作** —— 包括不清理旧编号。
  想让二级标题既有编号又清残留，请关掉该级的「跳过编号」。

## 1.1.0

**新增：重置当前笔记标题编号**

- 新命令，去掉标题上的编号、只留标题文字。`#### 3.1.1.1 (1) 测试四级标题` → `#### 测试四级标题`。
- 匹配规则：删掉「最后一个空格及其前面的部分」，但紧贴行尾的空格不算；删除前会校验被删部分确实含数字/字母/中文数字/罗马数字，且去掉中文数字后不再含中文，避免误删标题文字。
- 改完同样同步全库指向这些标题的链接。

**新增：自动去除旧编号（设置项，默认关闭）**

- 打开后，编号之前先去掉标题里以空格分隔的旧编号，再重新编号并比对，避免旧编号被夹在新编号中间。
- 顺带修掉「换了间隔符/规则之后旧编号会叠加」这个老问题。

**新增：每级可选编号序列**

- 一至六级各有独立下拉框：阿拉伯数字、罗马数字（贪心）、汉字数字、大写汉字数字、小写字母、大写字母（字母超过 `z`/`Z` 回到开头）。
- 罗马数字与汉字数字按标准写法生成：`4` → `IV`、`1990` → `MCMXC`、`101` → `一百零一`、`10000` → `一万`、`10001` → `一万零一`。
- 各级可混用，例如一级罗马 + 二级汉字 + 三级大写汉字 → `# I、 甲` / `## I.一 乙` / `### I.一.壹 丙`。
- 剥离旧编号时会按已启用的序列放宽识别范围（只配阿拉伯数字时不会去动 `IV 讨论` 这类标题）。

**修复**

- 剥离编号的正则首段漏了 `+`，导致**首段是多位数**的编号剥不掉：`# 2024 年度总结` 会被编成 `# 1、 2024 年度总结`。现已修正并补了回归断言。
- 一级标题的默认尾缀在首次安装以外的场景可能被清空（`loadSettings` 在设置里缺字段时没有回退到默认值）。

**文案**

- 设置项文案改用准确说法：编号与标题内容之间的那个东西叫**分隔符**（关闭尾缀时就是一个空格）。
- 新增中文/繁体/英文三语的「自动去除旧编号」「编号序列」等文案。

## 1.0.0

首个版本。

**编号**

- 一至六级标题自动编号，每 10 秒扫描当前笔记（可关闭，默认关闭）。
- 逐级可配：跳过编号、前后缀括符（`(|)`、`《|》`、`{(|>}`、不配套的 `(|」` 等）、编号层级间隔符（`.` `-` `:` `to`，各级可不同）、编号尾缀。
- 全局编号尾缀可一键覆盖各级设置，各级控件随之变为不可编辑。
- 标题下一行写 `%%skip%%` 即可让该标题完全不被触碰。
- 幂等：已编好的笔记反复扫描不变；旧编号被替换而不是叠加。
- 所有改动在一个编辑事务里提交，一次 `Ctrl+Z` 整体撤销。

**链接同步**

- 编号变化时同步全库 `[[笔记#旧标题]]` 与 `[文字](笔记.md#旧标题)`，效果与官方「重命名小标题」一致；Canvas 嵌入卡片一并更新。
- 复刻 Obsidian 官方的改写行为，并用驱动官方模态框实跑抓到的数据锁定了 8 种链接形态（含 `\|` 转义、`![[…]]` 嵌入、URL 形式的 `%20` 转义）。
- 链接指向别的同名笔记时不改（按链接解析结果判断归属）。
- 取不到内部接口时自动降级为「只编号、不动链接」。

**界面**

- 语言：简体中文 / 繁體中文 / English。
- 设置面板按「全局尾缀 + 六级标题」分组，各等级 4 个子设置。
- 命令：立即编号当前笔记。

**安全边界**

- 标题只从 Obsidian 的标题缓存取，代码块里的 `#` 不会被编号。
- 编辑器内容与缓存不一致（正在输入）时整轮放弃，不写一个字符。
- 不替换、不劫持官方命令。
